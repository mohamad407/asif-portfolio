// If your frontend and backend are deployed SEPARATELY (e.g. frontend on
// Vercel, backend on Render), set this to your backend's full URL:
//
//   window.PORTFOLIO_API_BASE = "https://your-backend.onrender.com";
//
// If they're deployed TOGETHER (same domain serves both), set this to "" —
// the app will then default to same-origin requests automatically.
//
// NOTE: this was pre-filled with a Render URL found in your old code.
// Confirm this is YOUR actual Render backend URL (check your Render
// dashboard) before deploying — update or clear it if it's wrong/unused.
window.PORTFOLIO_API_BASE = "https://asif-portfolio-egl8.onrender.com";
